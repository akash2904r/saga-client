import { Footer, Hero, Posts } from "./";

const Home = () => {
    return (
        <main className="w-full h-fit">
            <Hero />
            <Posts />
            <Footer />
        </main>
    );
};

export default Home;
